# G4 drill 2.1 — uncertain payment (Daraja timeout)

- **Executed:** 2026-09-22T14:06:50.867Z
- **Target:** `http://127.0.0.1:18080/api/payments`
- **Operator:** Nebyat (local-stack validation run)
- **Commit:** `5cf6eae`

Every block below is the real response, captured as it happened.

### 1. Force the timeout: POST /charges for KES 103

`2026-09-22T14:06:50.938Z`

The adapter's own timeout has to elapse before this returns — that wait IS the drill.

**POST /charges (took 4s)**

```
{"chargeId":"b390aa43-e0e3-4da4-80cc-806f8e627a66","saleId":"f17ad7d4-e8c5-43d0-b213-91fa43a49180","status":"PENDING","amountMinor":10300,"checkoutRequestId":null,"created":true}
HTTP 201
```

✅ **I5:** charge `b390aa43-e0e3-4da4-80cc-806f8e627a66` is `PENDING` with no CheckoutRequestID. There is no code path from a timeout to FAILED.


### 2. Retry the same sale: POST /charges again with the same saleId

`2026-09-22T14:06:55.044Z`

**POST /charges (retry)**

```
{"chargeId":"b390aa43-e0e3-4da4-80cc-806f8e627a66","saleId":"f17ad7d4-e8c5-43d0-b213-91fa43a49180","status":"PENDING","amountMinor":10300,"checkoutRequestId":null,"created":false}
HTTP 200
```

✅ **I2:** the retry returned the same charge, `created: false`. One push reached the provider, not two.

**GET /admin/charges/b390aa43-e0e3-4da4-80cc-806f8e627a66/audit — stkAttempts must still be 1**

```
{"chargeId":"b390aa43-e0e3-4da4-80cc-806f8e627a66","status":"PENDING","checkoutRequestId":null,"holdReason":null,"stkAttempts":1,"reconcileAttempts":0,"callbackEvents":[],"outboxEvents":[]}
HTTP 200
```


### 3. Reconcile until the reconciler gives up — and STILL does not fail the charge

`2026-09-22T14:06:55.130Z`

A charge with no CheckoutRequestID is `unqueryable`: there is nothing to ask Daraja about. Each look is counted. Past `RECONCILE_MAX_ATTEMPTS` it surfaces for a human via `/admin/pending` — it is never auto-failed. Looping the reconciler until that happens (max 15 passes):

**pass 3: POST /admin/reconcile**

```
{"examined":1,"resolvedPaid":0,"resolvedFailed":0,"stillPending":0,"unqueryable":1,"needsAttention":1,"errors":0}
HTTP 200
```

**pass 3: GET /admin/pending — the charge has crossed the threshold and is surfaced for a human**

```
{"count":1,"charges":[{"chargeId":"b390aa43-e0e3-4da4-80cc-806f8e627a66","saleId":"f17ad7d4-e8c5-43d0-b213-91fa43a49180","tenantId":"85c99c80-d05a-4c6d-96bf-b34a2b720198","amountMinor":10300,"checkoutRequestId":null,"reconcileAttempts":3,"holdReason":null,"situation":"the STK push timed out; no CheckoutRequestID to query","createdAt":"2026-09-22T14:06:50.953Z"}]}
HTTP 200
```

Situation reported: **the STK push timed out; no CheckoutRequestID to query**

**GET /charges/b390aa43-e0e3-4da4-80cc-806f8e627a66 after the reconciler gave up**

```
{"chargeId":"b390aa43-e0e3-4da4-80cc-806f8e627a66","saleId":"f17ad7d4-e8c5-43d0-b213-91fa43a49180","status":"PENDING","amountMinor":10300,"checkoutRequestId":null,"created":false}
HTTP 200
```

✅ **I5, the hard half:** 3 reconcile passes, the reconciler has given up asking, the charge is surfaced for a human — and it is STILL `PENDING`. Nothing guessed. That is the one unrecoverable mistake this service refuses to make.


### 4. Trace

`2026-09-22T14:06:55.364Z`

In X-Ray, filter on `annotation.payments.charge_id = "b390aa43-e0e3-4da4-80cc-806f8e627a66"` — one `POST /charges` segment with the outbound Daraja subsegment ending in a timeout, one short `POST /charges` segment for the retry with NO Daraja subsegment, one `POST /admin/reconcile` segment. Paste the trace id here:

trace_id: _____________________


---

**Drill wall-clock: 0m 05s.** Result: PASS — I2 and I5 held against the target above
